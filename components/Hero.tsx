import HeroContent from "./hero/HeroContent";

const Hero = () => {
  return (
    <section
      id="home"
      className={`justify-center py-12`}
    >
      <HeroContent />
    </section>
  );
};

export default Hero;
