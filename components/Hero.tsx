import HeroContent from "./hero/HeroContent";

const Hero = () => {
  return (
    <section
      id="home"
      className={`justify-center py-12 min-h-screen`}
    >
      <HeroContent />
    </section>
  );
};

export default Hero;
